# 远端 IP 或 ~/.ssh/config 中的 Host 名称，使用前填写。
SSH_HOST=""
SSH_PORT=22
SSH_USER="root"

# 本地 SSH 私钥路径；留空则使用 SSH 默认身份或 ssh-agent。
SSH_KEY="$HOME/.ssh/id_ed25519"

# 远端存放压缩包的临时目录（绝对路径），不存在时自动创建。
REMOTE_TMP_DIR="/tmp"
